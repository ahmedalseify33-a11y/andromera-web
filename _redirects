# Cloudflare Pages _redirects
# Incoming paths must be relative (starting with /).
# Domain-level canonicalization (www.andromera.com -> andromera.com) is managed via Cloudflare Dashboard Page Rules / Bulk Redirects.

/home / 301
